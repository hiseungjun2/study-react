let jwt;_451‍.x([["default",()=>_451‍.o]]);_451‍.w("jsonwebtoken",[["default",["jwt"],function(v){jwt=v}]]);

const jstMiddleware = (ctx, next) => {
    const token = ctx.cookies.get('access_token');
    if (!token) return next();  // 토큰이 없음
    try {
        const decoded = jwt.verify(token, process.env.JWT_SECRET);
        ctx.state.user = {
            _id : decoded._id,
            username : decoded.username
        }
        _451‍.g.console.log(decoded);
        return next();
    } catch (e) {
        // 토큰 검증 실패
        return next();
    }
};

_451‍.d(jstMiddleware);